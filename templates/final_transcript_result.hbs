<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <title>Final Transcript</title>
    <style>
        html {
            height: 100%;
        }

        body {
            display: flex;
            flex-direction: column;
            min-height: 100%;
            font-family: 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            font-size: 10pt;
            color: #333;
            line-height: 1.5;
            margin: 0;
        }

        .container {
            flex: 1 0 auto;
            padding: 40px;
        }

        .header {
            text-align: center;
            margin-bottom: 30px;
        }

        h1 {
            margin: 0;
            font-size: 20pt;
            font-weight: bold;
            padding-bottom: 10px;
            border-bottom: 2px solid #000;
        }

        .student-info {
            margin-bottom: 25px;
            padding: 15px;
            border: 1px solid #ddd;
            border-radius: 5px;
        }

        .student-info p {
            margin: 5px 0;
            font-size: 11pt;
        }

        .summary-section {
            margin-bottom: 30px;
            padding: 10px;
            text-align: center;
            border: 1px solid #ccc;
            border-radius: 5px;
        }

        .summary-section p {
            font-size: 16pt;
            font-weight: bold;
            margin: 0;
        }

        .transcript-table {
            width: 100%;
            border-collapse: collapse;
        }

        .transcript-table th,
        .transcript-table td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
            vertical-align: top;
        }

        .transcript-table thead th {
            background-color: #f2f2f2;
            font-weight: bold;
            text-align: center;
        }

        .block-row td {
            background-color: #e9ecef;
            font-weight: bold;
            font-size: 11pt;
        }

        .subject-row td {
            background-color: #f8f9fa;
        }

        .subject-row td:first-child {
            padding-left: 25px;
            font-style: italic;
        }

        .test-row td:first-child {
            padding-left: 50px;
        }

        .item-type {
            font-weight: bold;
            margin-right: 5px;
            font-style: normal;
        }

        .footer {
            flex-shrink: 0;
            text-align: center;
            padding: 20px 40px;
            font-size: 8pt;
            color: #888;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="header">
            <h1>Final Transcript</h1>
        </div>

        <div class="student-info">
            <p><strong>Student Name:</strong> {{student.first_name}} {{student.last_name}}</p>
            <p><strong>Student ID:</strong> {{student._id}}</p>
            <p><strong>Date of Birth:</strong> {{formatDate student.date_of_birth}}</p>
            <p><strong>School:</strong> {{student.school.commercial_name}}</p>
        </div>

        <div class="summary-section">
            <p>Overall Result: {{overall_result}}</p>
        </div>

        <table class="transcript-table">
            <thead>
                <tr>
                    <th>Syllabus Component</th>
                    <th>Result</th>
                    <th>Final Mark</th>
                </tr>
            </thead>
            <tbody>
                {{#each block_results}}
                <tr class="block-row">
                    <td><span class="item-type">Block:</span>{{block.name}}</td>
                    <td>{{block_result}}</td>
                    <td>{{toFixed block_total_mark 2}}</td>
                </tr>
                {{#each subject_results}}
                <tr class="subject-row">
                    <td><span class="item-type">Subject:</span>{{subject.name}}</td>
                    <td>{{subject_result}}</td>
                    <td>{{toFixed subject_total_mark 2}}</td>
                </tr>
                {{#each test_results}}
                <tr class="test-row">
                    <td><span class="item-type">Test:</span>{{test.name}}</td>
                    <td>{{test_result}}</td>
                    <td>{{toFixed test_total_mark 2}}</td>
                </tr>
                {{/each}}
                {{/each}}
                {{/each}}
            </tbody>
        </table>
    </div>

    <div class="footer">
        <p>Transcript generated on: {{formatDate updated_at}}</p>
    </div>
</body>

</html>